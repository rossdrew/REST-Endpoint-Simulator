language: python
python:
  - "3.5"
# command to install dependencies
install: 
  - pip install --upgrade setuptools
  - pip install web.py
  - pip install operator
# command to run tests
script: python -m doctest endpointSim.py
